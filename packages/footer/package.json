{
  "name": "footer",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && rollup --config",
    "test": "jest"
  },
  "dependencies": {
    "typescript": "^4.5.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
